{
  "name": "secret-santa-fixer",
  "version": "1.0.0",
  "description": "Fixes a broken Secret Santa draw.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@sendgrid/mail": "^7.7.0"
  }
}